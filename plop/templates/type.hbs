/*=============================================== {{>pascalName}} ===============================================*/

{{#if interface}}
export interface {{>pascalName}} {

}
{{ else }}
export type {{>pascalName}} = {
    
}
{{/if}}